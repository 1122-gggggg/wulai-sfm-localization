# 候補定位方法

非正式的定位方法索引。正式路徑是 EDM，見 `../定位演算法/`。

`演算法實作索引/` 是符號連結，核心程式保留在原本的 import 路徑：

| 連結 | 指向 |
|---|---|
| `EDM_ONNX_TensorRT_matcher.py` | `定位演算法/deploy_code/sfm_glomap_deploy/edm_onnx_matcher.py` |
| `NeuFlow_refresh_experiment.py` | `定位演算法/validation/neuflow_refresh_experiment.py` |
| `Projection_guided_tracker.py` | `定位演算法/validation/projection_guided_tracker.py` |

XFeat / LighterGlue 的設定、啟動腳本與場域資產已於 2026-07-26 移除；
tracker 程式碼仍保留在 `定位演算法/deploy_code/sfm_glomap_deploy/` 與
`定位演算法/flight_control/`。
