# Pinned versions of the original machine's localization runtime interpreter
# (/usr/bin/python3.12, CUDA 12.8 wheels). Match the CUDA suffix to the new
# machine's driver; the pycolmap pin matters most (rig-format map compat).
torch==2.11.0+cu128
torchvision==0.26.0+cu128
numpy==2.4.6
opencv-python==4.13.0
pillow==10.2.0
pycolmap==4.0.4
# Load-bearing for the XFeat / LighterGlue / MegaLoc torch.hub stack (were missing):
huggingface_hub==1.22.0
safetensors==0.8.0
kornia==0.8.2
# System-level (not pip): ffmpeg, tkinter (python3-tk)
# XFeat / LighterGlue / MegaLoc: torch.hub, offline cache in torch_hub_cache/
# Optional: scipy enables the sparse-cloud collision monitor in
# real_path_follow_controller (guarded import; absent = monitor unavailable)
# scipy==1.17.1
